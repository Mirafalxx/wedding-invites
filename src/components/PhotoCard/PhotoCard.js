import React from 'react';
import { Image } from 'antd';
import './style.scss';

const PhotoCard = ({ src, alt }) => {
  return <div className="photo-item">Максим</div>;
};

export default PhotoCard;
